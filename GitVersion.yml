next-version: 2.3.0

mode: ContinuousDelivery

branches:
  main:
    regex: ^main$
    label: ''
    increment: Patch
    is-release-branch: true
    source-branches:
      - develop
    prevent-increment:
      of-merged-branch: true

  develop:
    regex: ^develop$
    label: beta
    increment: Minor
    is-main-branch: true
    track-merge-target: true
    source-branches:
      - main

  feature:
    regex: ^feature[/-]
    label: alpha
    increment: Inherit
    source-branches:
      - develop
    
  hotfix:
    regex: ^hotfix[/-]
    label: ''
    increment: Patch
    source-branches:
      - main

ignore:
  sha: []
